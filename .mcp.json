{
  "mcpServers": {
    "vigil": {
      "command": ".venv/bin/vigil",
      "args": [],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}",
        "VIGIL_MAX_ROWS": "100",
        "VIGIL_SAMPLE_SIZE": "3",
        "VIGIL_STATEMENT_TIMEOUT_MS": "5000"
      }
    }
  }
}
